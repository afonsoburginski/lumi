{
  "name": "lumi",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "mobile": "bun run --cwd apps/mobile start",
    "api": "bun run --cwd apps/api dev",
    "test": "bun test",
    "typecheck": "bun run --cwd apps/mobile typecheck && bun run --cwd packages/shared typecheck"
  }
}
