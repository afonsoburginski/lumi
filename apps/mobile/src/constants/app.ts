/** Constantes globais do app. Específicas de um mod ficam em features/<mod>/constants.ts. */

export const APP_NAME = 'Lumi';

/** Limite diário de leituras do visitante (ver features/auth). */
export const GUEST_DAILY_LIMIT = 3;

/** Tempo (ms) até o chrome do player sumir sozinho. */
export const PLAYER_CHROME_TIMEOUT_MS = 3500;
